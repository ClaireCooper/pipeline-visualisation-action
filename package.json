{
  "name": "pipeline-visualisation-action",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "ncc build src/index.ts -o dist --source-map --license licenses.txt",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@actions/artifact": "^6.2.0",
    "@actions/core": "^1.10.1",
    "@octokit/rest": "^21.0.2",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.0.0",
    "@vercel/ncc": "^0.38.3",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
